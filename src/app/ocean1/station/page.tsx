"use client";

export default function StationPage() {
  return (
    <div className="relative w-screen h-screen overflow-hidden bg-[#030308] cursor-default select-none flex items-center justify-center">
      <p
        className="text-white/30 text-sm tracking-[0.3em]"
        style={{ fontFamily: "var(--font-display)" }}
      >
        STATION
      </p>
    </div>
  );
}
